<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production: cleartext disabled. Only HTTPS traffic is permitted. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust only system certificates (no user-added CAs) -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Domain-specific: still enforce HTTPS -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">linguagt.com</domain>
    </domain-config>

    <!-- Debug overrides: allow cleartext + user CAs ONLY in debug builds.
         Android automatically ignores this block in release builds. -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
