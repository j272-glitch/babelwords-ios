# Replace these placeholders or set the corresponding FASTLANE_* environment variables.
app_identifier(ENV["FASTLANE_APP_IDENTIFIER"] || "com.babelwords.LinguaVibe")
apple_id(ENV["FASTLANE_APPLE_ID"] || "your-apple-id@example.com")
team_id(ENV["FASTLANE_TEAM_ID"] || "YOUR_TEAM_ID")
